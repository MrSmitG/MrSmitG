"""Local Social Agent - a local-first AI agent that runs on your own machine.

The package is intentionally dependency-light so it starts instantly on macOS and
degrades gracefully when optional pieces (Ollama, instagrapi, diffusers) are absent.
"""

__version__ = "0.1.0"
